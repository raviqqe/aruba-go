name: raviqqe/aruba-go
description: Run Aruba in Go
branding:
  icon: aperture
  color: red
inputs: {}
outputs: {}
runs:
  using: composite
  steps:
    - run: go -C "$GITHUB_ACTION_PATH" run ./cmd/agoa --strict "$GITHUB_WORKSPACE/features"
      shell: bash
